{
    "version": "0.2.0",
    "configurations": [
        {
            "preLaunchTask": "start",
            "type": "chrome",
            "request": "launch",
            "name": "Run",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}